<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="text-center p-4 border rounded-3 bg-light">
                <div class="display-4 text-danger mb-3">
                    <i class="bi bi-exclamation-triangle"></i>
                </div>
                <h1 class="h3 mb-3">Không tìm thấy lớp học</h1>
                <p class="text-muted mb-4">
                    Lớp với mã <strong><%= typeof classId !== 'undefined' ? classId : (pageData && pageData.classId ? pageData.classId : '') %></strong> không tồn tại hoặc bạn không có quyền truy cập.
                </p>
                <a class="btn btn-primary" href="/classes">
                    <i class="bi bi-arrow-left-short"></i>
                    Quay lại danh sách lớp
                </a>
            </div>
        </div>
    </div>
</div>
